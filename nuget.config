<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- <clear /> intentionally commented out: let inherited sources (machine / user
         level on the Mend scan host) remain active so we can observe whether
         nuget.org gets pulled in as a fallback. -->
    <add key="PIC-UK-TeamFeed" value="https://nexus.mendinfra.com/repository/dotnet-feed/index.json" />
  </packageSources>
</configuration>
